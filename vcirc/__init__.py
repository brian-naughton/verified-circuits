"""verified-circuits: kernel-checkable mechanistic circuits for learned transformers.

Train a tiny transformer on a complete finite task, extract a human-readable
symbolic circuit, and ship independently-checkable evidence that
`Spec == Circuit == Model` over the *entire* input domain.
"""

__version__ = "0.0.1"
